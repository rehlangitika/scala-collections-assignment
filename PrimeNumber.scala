/*
object PrimeNumber extends App {
def findPrime(srcList:List[Int]):List[Int]]={

srcList.map(n=>{
   val l=for{
    i<-1 to n/2
    if(n%i!=0)
  }yield n
  l.toList
})
  }
  print(findPrime(List(2,3,4,5,6,7)))

}
*/